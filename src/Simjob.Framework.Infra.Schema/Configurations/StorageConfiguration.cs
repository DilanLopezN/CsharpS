using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Options;

namespace Simjob.Framework.Infra.Schemas.Configurations
{
    public static class StorageConfiguration
    {
        //public static void AddStorageConfiguration(this IServiceCollection services,IConfiguration configuration)
        //{
        //    var storageConfiguration = new StorageConfigurations();
        //    new ConfigureFromConfigurationOptions<StorageConfigurations>(
        //            configuration.GetSection("StorageConfigurations"))
        //        .Configure(storageConfiguration);
        //    services.AddSingleton(storageConfiguration);
        //}
    }
}
